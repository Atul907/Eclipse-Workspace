package variables;

public class Arithmetic {

	//Here is a simple Java program that performs arithmetic operations using just one variable:

		    public static void main(String[] args) {
		        int a = 5;
		        System.out.println("a + a = " + (a + a));
		        System.out.println("a - a = " + (a - a));
		        System.out.println("a * a = " + (a * a));
		        System.out.println("a / a = " + (a / a));
		    }
		
  //This program uses only one variable `a` to perform addition, subtraction, multiplication & division.
}
